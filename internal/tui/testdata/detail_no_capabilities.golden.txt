#111 · Widget sync v2: shards, retries & telemetry                                                         read just now
#112  Draft the shard sync protocol                                        https://tracker.example.test/acme/widgets/112
[In Review]  @mara-vos  #501 open ci ok changes req

DESCRIPTION

The shard sync protocol has to survive a shard going away mid-transfer & coming back with a different generation number.

## Shape

1. The sender announces `(shard, generation, offset)`.
2. The receiver either accepts or replies with the offset it wants.
3. Either side may abort; an aborted transfer leaves no partial state.

Open question: do we version the handshake itself, or rely on the generation number to carry it? Leaning towards an
explicit version — see the linked ticket.

LINKS (1)

is duplicated by  #7  Teach the gadget agent to speak sync v2  [In Progress]



















m off · shift-drag to select text • esc back • q quit • ↑/k up • ↓/j down • u epic • r refresh • ? help
